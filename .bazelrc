# Use local Python toolchain from venv
build --action_env=PYTHONPATH=
build --action_env=PYTHONNOUSERSITE=1

# Register cross-compilation toolchains
build --extra_toolchains=//toolchains/arm64-linux-gnu/bbai64:bbai64_toolchain

# Custom build configurations
# ARM64 build for BeagleBone AI-64
build:arm64 --platforms=//toolchains:linux_aarch64

# Host build (x86_64)
build:host --platforms=//toolchains:linux_x86_64

# Release builds with optimizations
build:release --compilation_mode=opt
build:release --strip=always

# Combined configs
build:release-arm64 --config=arm64
build:release-arm64 --config=release

build:release-host --config=host
build:release-host --config=release
